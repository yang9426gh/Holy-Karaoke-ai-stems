Put Windows vendor binaries here for offline packaging:

- ffmpeg.exe (and ffprobe.exe if needed)
- yt-dlp.exe (optional; you can also use python yt-dlp)
- rubberband.exe (optional; for higher-quality pitch shifting)

These will be bundled into the PyInstaller output via pyinstaller_backend.spec.
